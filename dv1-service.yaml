apiVersion: v1
kind: Service
metadata:
  name: service-dv1
  namespace: ns-dv1
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30443
      protocol: TCP
  selector:
    app: service-dv1